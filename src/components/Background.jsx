import imageBackground from "../images/background-login.png";

const Background = () => {
  return (
    <><img
    src={imageBackground}
    alt="imageBackground"
    className="w-full h-full fixed left-0 top-0 bg-no-repeat -z-50"
  /></>
  )
}

export default Background
